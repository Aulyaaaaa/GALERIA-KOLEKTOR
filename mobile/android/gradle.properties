org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Proyek ini di drive D: sementara Pub Cache Flutter ada di drive C: --
# Kotlin incremental compiler crash ("this and base files have different
# roots") saat menghitung path relatif lintas-drive Windows utk source
# plugin native (mis. share_plus). Nonaktifkan incremental compilation
# Kotlin supaya build tidak jatuh ke fallback lambat/berisik tiap kali.
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
